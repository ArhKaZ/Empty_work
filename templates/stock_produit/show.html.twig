{% extends 'base.html.twig' %}

{% block title %}StockProduit{% endblock %}

{% block body %}
    <h1>StockProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_produit.id }}</td>
            </tr>
            <tr>
                <th>Quantitee</th>
                <td>{{ stock_produit.quantitee }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ stock_produit.date ? stock_produit.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ stock_produit.commentaire }}</td>
            </tr>
            <tr>
                <th>Origine</th>
                <td>{{ stock_produit.origine }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stock_produit_index') }}">back to list</a>

    <a href="{{ path('stock_produit_edit', {'id': stock_produit.id}) }}">edit</a>

    {{ include('stock_produit/_delete_form.html.twig') }}
{% endblock %}
