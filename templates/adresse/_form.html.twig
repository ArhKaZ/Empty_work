{{ form_start(form) }}
<div class="row mb-3">
    <div class="col">{{ form_row(form.num) }}</div>
    <div class="col">{{ form_row(form.rue) }}</div>
    <div class="col">{{ form_row(form.ville) }}</div>
</div>
<div class="row mb-2"></div>
    <div class="col">{{ form_row(form.code_postal) }}</div>
    <div class="col">{{ form_row(form.pays) }}</div>
</div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
