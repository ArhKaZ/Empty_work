{% extends 'base.html.twig' %}

{% block title %}Taille{% endblock %}

{% block body %}
    <h1>Taille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taille.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ taille.nom }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ taille.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('taille_index') }}">back to list</a>

    <a href="{{ path('taille_edit', {'id': taille.id}) }}">edit</a>

{% endblock %}
