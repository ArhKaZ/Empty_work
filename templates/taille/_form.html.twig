{% form_theme form with [
    'bootstrap_4_layout.html.twig',
    'article/_form_theme.html.twig'
] %}
{{ form_start(form) }}
<div class="row">
    <div class="col">
        {{ form_row(form.nom) }}
        {{ form_row(form.commentaire) }}
        {{ form_row(form.quantite_tissu, {'label': 'Quantité tissu *'}) }}
        <div class="mt-3">
            <button class="btn btn-primary">Enregistrer</button>
            <a href="{{ path('produit_index') }}" class="btn btn-secondary">Retour</a>
        </div>
    </div>
    <div class="col-7">
        {{ form_row(form.utiliseMPs) }}
    </div>
</div>
{{ form_end(form) }}
