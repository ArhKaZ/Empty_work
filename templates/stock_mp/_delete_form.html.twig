<form method="post" action="{{ path('stock_m_p_delete', {'id': stock_m_p.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_m_p.id) }}">
    <button class="btn">Delete</button>
</form>
